# Streamdevice examples

## Connection check
This examples create a heart beat PV which will increase the count if the TCP/IP connection to a specific host and port is alive, and will stop if not. If the connection come back alive again it will return the count

### Files 
* heart.proto
* heart.db
* st_heart.cmd
